
/* db user oluşturmak */
--exec sys.sp_addlogin TABIM, 312, Surucu07, Türkçe
--exec sys.sp_grantdbaccess TABIM, TABIM
--exec sys.sp_addrolemember 'db_owner', TABIM
--exec sys.sp_addsrvrolemember TABIM, 'sysadmin'


-- data transferini başlatmak için
      Update [dbo].[DbUpdates] set IsActive = 1 Where MsDbUpdateId = -1 and UpdateTypeId = 13 and DBaseNoTypeId = 6

-- data transferini kapatmak için
      Update [dbo].[DbUpdates] set IsActive = 0 Where MsDbUpdateId = -1 and UpdateTypeId = 13 and DBaseNoTypeId = 6



/*  Sistemden tabloları sorgulamalar */

Select * From sys.objects
Where name like '%Mtsk%'
and schema_id = 1
and ( type_desc = 'USER_TABLE'
   or type_desc = 'SQL_STORED_PROCEDURExxxxx')

order by modify_date desc



/* ldf dosyası bozulan database kurtarmak için */

EXEC sp_attach_single_file_db @dbname = 'Surucu07', @physname = N'C:\SqlData\MenderesMtsk\Surucu07.mdf';


/*   varchar(max) olan bir kolonun tüm detayını görmek için    */

SELECT CAST(SqlScript AS XML) FROM [UstadManagerV3].[dbo].[MsProjectTables] where TableName = 'DbUpdates'
SELECT SqlScript AS [XML_F52E2B61-18A1-11d1-B105-00805F49916B] FROM [UstadManagerV3].[dbo].[MsProjectTables] where TableName = 'DbUpdates'
SELECT SqlScript AS [processing-instruction(x)] FROM [UstadManagerV3].[dbo].[MsProjectTables] where TableName = 'DbUpdates' FOR XML PATH('')


/*  dikkat MSSQL JSON komutları çalışmadğı zaman aşağıdaki komutu çalıştırmanız gerekiyor */

ALTER DATABASE DatabaseName SET COMPATIBILITY_LEVEL = 130
ALTER DATABASE MSV3DFTRBLT SET COMPATIBILITY_LEVEL = 130


/* Identitiy field ON-OFF */

SET IDENTITY_INSERT [dbo].[EduMtskYonetmelik] ON 
SET IDENTITY_INSERT [dbo].[EduMtskYonetmelik] OFF

 ***** TABLO KOPYALAMAK **************

Select * into EduMtskYonetmelik2 from EduMtskYonetmelik


 /* tablolara ait kaç adet trigger mevcut  */
 Select tbl.name, count(trg.object_id) adet From u7093888_Ustad01.sys.triggers as trg 
   left outer join u7093888_Ustad01.sys.tables as tbl on ( trg.parent_id = tbl.object_id ) 
 group by tbl.name



/* ***************  TABLES ve FIELDS tablosundaki xxx_CODE değişikliği ************* */


--//( 3, 'UST', 'PMS', 'PMS', 'UST.PMS.PMS', '', 'Project Managment',  1 )  

USE [MSV3DFTRBLT]
GO

declare @oldTableCode varchar(50) = 'MsSectorTables'
declare @newTableCode varchar(50) = 'MsProjectTables'

select * from MS_TABLES 
where TABLE_CODE = @oldTableCode

/*
update MS_TABLES set
-- MODUL_CODE = 'PMS'
--,SOFTWARE_CODE = 'UST'
--,PROJECT_CODE = 'PMS'
   TABLE_CODE = @newTableCode
where TABLE_CODE = @oldTableCode
*/


select * from MS_FIELDS 
where TABLE_CODE = @oldTableCode

/*
update MS_FIELDS set
-- SOFTWARE_CODE = 'UST'
--,PROJECT_CODE = 'PMS'
   TABLE_CODE = @newTableCode
where TABLE_CODE = @oldTableCode
*/

select * from MS_TABLES_IP
where TABLE_CODE = @oldTableCode

/*
update MS_TABLES_IP set
-- SOFTWARE_CODE = 'UST'
--,PROJECT_CODE = 'PMS'
   TABLE_CODE = @newTableCode
where TABLE_CODE = @oldTableCode
*/

select * from MS_FIELDS_IP 
where TABLE_CODE = @oldTableCode

/*
update MS_FIELDS_IP set
-- SOFTWARE_CODE = 'UST'
--,PROJECT_CODE = 'PMS'
   TABLE_CODE = @newTableCode
where TABLE_CODE = @oldTableCode
*/



GO


/*  IP_CODE değişikliği yukarının devamı ikinci adımı */ 



select * from MS_TABLES_IP
where TABLE_CODE = 'User'
and IP_CODE = 'SYSUSER_L01'
/*
update MS_TABLES_IP set
 IP_CODE = 'UserList_L01'
where TABLE_CODE = 'User'
and IP_CODE = 'SYSUSER_L01'

*/


select * from MS_FIELDS_IP 
where TABLE_CODE = 'User'
and IP_CODE = 'SYSUSER_L01'
/*
update MS_FIELDS_IP set
 IP_CODE = 'UserList_L01'
where TABLE_CODE = 'User'
and IP_CODE = 'SYSUSER_L01'

*/




*********** SOFTWARE_CODE set işlemi ******************


declare @tableCode varchar(30) = 'UstadFirms'

select * from MS_TABLES
where TABLE_CODE = @tableCode

select * from MS_TABLES_IP
where TABLE_CODE = @tableCode

/*
update MS_TABLES Set
 SOFTWARE_CODE = 'UST'
,PROJECT_CODE = 'CRM'
,MODUL_CODE = 'ABO'
where TABLE_CODE = @tableCode

update MS_TABLES_IP Set
 SOFTWARE_CODE = 'UST'
,PROJECT_CODE = 'CRM'
where TABLE_CODE = @tableCode
*/


select * from MS_FIELDS
where TABLE_CODE = @tableCode

select * from MS_FIELDS_IP
where TABLE_CODE = @tableCode

/*
update MS_FIELDS Set
 SOFTWARE_CODE = 'UST'
,PROJECT_CODE = 'CRM'
where TABLE_CODE = @tableCode


update MS_FIELDS_IP Set
 SOFTWARE_CODE = 'UST'
,PROJECT_CODE = 'CRM'
where TABLE_CODE = @tableCode
*/




 **** FIELD NO DEĞİŞTİRMEK ***********
 
DECLARE @TABLE_CODE VARCHAR(20)

DECLARE @old_FIELD_NO SMALLINT = 0
DECLARE @new_FIELD_NO SMALLINT = 0

SET @TABLE_CODE = 'FNLNVOL'

SET @old_FIELD_NO = 24
SET @new_FIELD_NO = 73

update MS_FIELDS_IP set
FIELD_NO = @new_FIELD_NO
where TABLE_CODE = @TABLE_CODE
and   FIELD_NO = @old_FIELD_NO  

update MS_FIELDS set
FIELD_NO = @new_FIELD_NO
where TABLE_CODE = @TABLE_CODE
and   FIELD_NO = @old_FIELD_NO  




 *** ARAYA FIELD EKLEMEK İÇİN ****

declare @tablo_kodu varchar(30) = 'MtskUygulamaliDers'
declare @softwareCode varchar(30) = 'UST'
declare @projectCode varchar(30) = 'MEB'

declare @field_no int = 6
declare @field_no_Sinir int = 100
declare @adim int = 1

UPDATE MS_FIELDS SET FIELD_NO = FIELD_NO + @adim
WHERE TABLE_CODE = @tablo_kodu
AND SOFTWARE_CODE = @softwareCode
AND PROJECT_CODE = @projectCode
AND FIELD_NO > @field_no
AND FIELD_NO < @field_no_Sinir

UPDATE MS_FIELDS_IP SET FIELD_NO = FIELD_NO + @adim
WHERE TABLE_CODE = @tablo_kodu
AND SOFTWARE_CODE = @softwareCode
AND PROJECT_CODE = @projectCode
AND FIELD_NO > @field_no
AND FIELD_NO < @field_no_Sinir


/* ************** TABLE_CODE değişikliği ******************* */

declare @tableCode varchar(50) = 'MtskTaslakUygulamaS'
declare @newTableCode varchar(50) = 'MtskSablonUygulamaS'


select * from MS_FIELDS_IP
where TABLE_CODE = @tableCode

select * from MS_TABLES_IP
where TABLE_CODE = @tableCode

select * from MS_FIELDS
where TABLE_CODE = @tableCode

select * from MS_TABLES
where TABLE_CODE = @tableCode

/*

update MS_FIELDS_IP 
set TABLE_CODE = @newTableCode
where TABLE_CODE = @tableCode

update MS_TABLES_IP
set TABLE_CODE = @newTableCode
where TABLE_CODE = @tableCode

update MS_FIELDS
set TABLE_CODE = @newTableCode
where TABLE_CODE = @tableCode

update MS_TABLES
set TABLE_CODE = @newTableCode
where TABLE_CODE = @tableCode

*/





/* *** SADECE BİR IP PLANIN FIELDLERİNİ SİLMEK *** */

DECLARE @IPCODE VARCHAR(20)
SET @IPCODE = 'FNLNVOL_BNL01'

delete from MS_FIELDS_IP
where IP_CODE = @IPCODE



/*** FIELD SİLMEK İÇİN ****/

DECLARE @TABLECODE VARCHAR(20)
SET @TABLECODE = 'AJLNTLK'

declare @field_no int = -1

delete from MS_FIELDS_IP
where TABLE_CODE = @TABLECODE
AND FIELD_NO = @field_no

delete from MS_FIELDS
where TABLE_CODE = @TABLECODE
AND FIELD_NO = @field_no




*** KOMPLE TABLO VE FIELDLERI SİLMEK İÇİN ****

DECLARE @TABLECODE VARCHAR(20)
SET @TABLECODE = 'HPFNS'

delete from MS_FIELDS_IP
where TABLE_CODE= @TABLECODE

delete from MS_FIELDS
where TABLE_CODE= @TABLECODE

delete from MS_TABLES_IP
where TABLE_CODE= @TABLECODE

delete from MS_TABLES
where TABLE_CODE= @TABLECODE




*** LAYOUT TABLOSUNDAN BİR FORM TASARIMINI SİLMEK İÇİN *************

DELETE FROM [dbo].[MS_LAYOUT]
WHERE MASTER_CODE = 'FNS_HP'


*** LAYOUT TABLOSUNDAN BİR FORMUN MASTER_CODE SİNİ DEĞİŞTİRMEK İÇİN *************

UPDATE [dbo].[MS_LAYOUT] SET
MASTER_CODE = 'CR_AV_ARA_F01'
WHERE MASTER_CODE = 'CR_ARA_F01'




*****  FIELD DEĞİŞİKLİĞİ ALAN/TYPE DEĞİŞİKLİĞİ *********************

ALTER TABLE SYS_TYPES_L ALTER COLUMN TYPES_CAPTION VARCHAR(200) NULL

*****  FIELD DEĞİŞİKLİĞİ İSİM DEĞİŞİKLİĞİ *********************

EXEC sp_RENAME 'TableName.OldColumnName' , 'NewColumnName', 'COLUMN'

EXEC sp_RENAME 'TableName.OldColumnName' , 'NewColumnName', 'COLUMN'

**********************************************

*****  FIELD EKLEME **************************

ALTER TABLE MS_LAYOUT ADD CMP_NAME VARCHAR(100) NULL

**********************************************


IP_CODE değişikliği 

-- 1. işlem 


-- FOREING_KEYS DELETE
USE [MSV3DFTR]
GO

-- ANA TABLE_CODE YİDE DEĞİŞTİRMEK İÇİN
IF  EXISTS (SELECT * FROM sys.foreign_keys WHERE object_id = OBJECT_ID(N'[dbo].[FK_MS_FIELDS_MS_TABLES]') AND parent_object_id = OBJECT_ID(N'[dbo].[MS_FIELDS]'))
ALTER TABLE [dbo].[MS_FIELDS] DROP CONSTRAINT [FK_MS_FIELDS_MS_TABLES]
GO

-- ANA TABLE_CODE YİDE DEĞİŞTİRMEK İÇİN
IF  EXISTS (SELECT * FROM sys.foreign_keys WHERE object_id = OBJECT_ID(N'[dbo].[FK_MS_TABLES_IP_MS_TABLES]') AND parent_object_id = OBJECT_ID(N'[dbo].[MS_TABLES_IP]'))
ALTER TABLE [dbo].[MS_TABLES_IP] DROP CONSTRAINT [FK_MS_TABLES_IP_MS_TABLES]
GO


IF  EXISTS (SELECT * FROM sys.foreign_keys WHERE object_id = OBJECT_ID(N'[dbo].[FK_MS_FIELDS_IP_MS_TABLES_IP]') AND parent_object_id = OBJECT_ID(N'[dbo].[MS_FIELDS_IP]'))
ALTER TABLE [dbo].[MS_FIELDS_IP] DROP CONSTRAINT [FK_MS_FIELDS_IP_MS_TABLES_IP]
GO

-- 2. işlem

DECLARE @old_IPCODE VARCHAR(20)
DECLARE @new_IPCODE VARCHAR(20)
SET @old_IPCODE = 'FNLNVOL_CRKS_F02'
SET @new_IPCODE = 'FNLNVOL_KSCR_F03'

update MS_FIELDS_IP set
IP_CODE = @new_IPCODE
where IP_CODE = @old_IPCODE

update MS_TABLES_IP set
IP_CODE = @new_IPCODE
where IP_CODE = @old_IPCODE

update [MS_GROUPS] SET 
[TABLE_CODE] = @new_IPCODE
WHERE [TABLE_CODE] = @old_IPCODE


--- ORRRRR ----------------

DECLARE @old_TABLECODE VARCHAR(20)
DECLARE @new_TABLECODE VARCHAR(20)

DECLARE @old_IPCODE VARCHAR(20)
DECLARE @new_IPCODE VARCHAR(20)

SET @old_TABLECODE = 'HCR'
SET @new_TABLECODE = 'HCRJ'

SET @old_IPCODE = 'HCR_LST04'
SET @new_IPCODE = 'HCRJ_LS01'

update MS_FIELDS_IP set
TABLE_CODE = @new_TABLECODE,
IP_CODE = @new_IPCODE
where TABLE_CODE = @old_TABLECODE
AND IP_CODE = @old_IPCODE

update MS_TABLES_IP set
TABLE_CODE = @new_TABLECODE,
IP_CODE = @new_IPCODE
where TABLE_CODE = @old_TABLECODE
AND IP_CODE = @old_IPCODE

UPDATE [MS_GROUPS] SET 
[TABLE_CODE] = @new_IPCODE
WHERE [TABLE_CODE] = @old_IPCODE

----- NEW ORRRRR ----
-- ANA TABLE_CODE YİDE DEĞİŞTİRMEK İÇİN

DECLARE @old_TABLECODE VARCHAR(20)
DECLARE @new_TABLECODE VARCHAR(20)

DECLARE @old_IPCODE VARCHAR(20)
DECLARE @new_IPCODE VARCHAR(20)

SET @old_TABLECODE = 'HUR'
SET @new_TABLECODE = 'HPURN'

SET @old_IPCODE = 'HUR_LST02' 
SET @new_IPCODE = 'HPURN_L02'

--SELECT DISTINCT IP_CODE FROM MS_FIELDS_IP
--WHERE TABLE_CODE = @old_TABLECODE 


update MS_FIELDS set
TABLE_CODE = @new_TABLECODE
where TABLE_CODE = @old_TABLECODE

update MS_TABLES set
TABLE_CODE = @new_TABLECODE
where TABLE_CODE = @old_TABLECODE

update MS_FIELDS_IP set
TABLE_CODE = @new_TABLECODE,
IP_CODE = @new_IPCODE
where TABLE_CODE = @old_TABLECODE
AND IP_CODE = @old_IPCODE

update MS_TABLES_IP set
TABLE_CODE = @new_TABLECODE,
IP_CODE = @new_IPCODE
where TABLE_CODE = @old_TABLECODE
AND IP_CODE = @old_IPCODE

UPDATE [MS_GROUPS] SET 
[TABLE_CODE] = @new_IPCODE
WHERE [TABLE_CODE] = @old_IPCODE




-- 3. işlem


-- FOREING_KEYS CREATE
USE [MSV3DFTR]
GO

ALTER TABLE [dbo].[MS_FIELDS]  WITH CHECK ADD  CONSTRAINT [FK_MS_FIELDS_MS_TABLES] FOREIGN KEY([TABLE_CODE])
REFERENCES [dbo].[MS_TABLES] ([TABLE_CODE])
GO

ALTER TABLE [dbo].[MS_FIELDS] CHECK CONSTRAINT [FK_MS_FIELDS_MS_TABLES]
GO


ALTER TABLE [dbo].[MS_TABLES_IP]  WITH CHECK ADD  CONSTRAINT [FK_MS_TABLES_IP_MS_TABLES] FOREIGN KEY([TABLE_CODE])
REFERENCES [dbo].[MS_TABLES] ([TABLE_CODE])
GO

ALTER TABLE [dbo].[MS_TABLES_IP] CHECK CONSTRAINT [FK_MS_TABLES_IP_MS_TABLES]
GO


ALTER TABLE [dbo].[MS_TABLES_IP]  WITH CHECK ADD  CONSTRAINT [FK_MS_TABLES_IP_MS_TABLES] FOREIGN KEY([TABLE_CODE])
REFERENCES [dbo].[MS_TABLES] ([TABLE_CODE])
GO

ALTER TABLE [dbo].[MS_TABLES_IP] CHECK CONSTRAINT [FK_MS_TABLES_IP_MS_TABLES]
GO



ALTER TABLE [dbo].[MS_FIELDS_IP]  WITH CHECK ADD  CONSTRAINT [FK_MS_FIELDS_IP_MS_TABLES_IP] FOREIGN KEY([IP_CODE])
REFERENCES [dbo].[MS_TABLES_IP] ([IP_CODE])
GO

ALTER TABLE [dbo].[MS_FIELDS_IP] CHECK CONSTRAINT [FK_MS_FIELDS_IP_MS_TABLES_IP]
GO



**********************************************

/*** MS_PROPERTIES Tablosundaki bir fieldin detay SQL i  ***/


declare @fname varchar(100)
set @fname = 'AUTO_LST' 

Select FIELDNAME, ROW_CAPTION, ROW_FIELDNAME, ROW_COLUMN_TYPE
from MS_PROPERTIES 
where 0 = 0
and FIELDNAME = @fname
and ROW_TYPE > 1
and ROW_FIELDNAME <> 'CAPTION'

union all

Select FIELDNAME, ROW_CAPTION, ROW_FIELDNAME, ROW_COLUMN_TYPE
from MS_PROPERTIES 
where 0 = 0
and ROW_TYPE > 1
and ROW_FIELDNAME <> 'CAPTION'
and FIELDNAME in
(
Select ROW_FIELDNAME from MS_PROPERTIES 
where 0 = 0
and FIELDNAME = @fname
and ( ROW_COLUMN_TYPE = 'tPropertiesEdit' 
   or ROW_COLUMN_TYPE = 'tPropertiesPlusEdit' )
)



**********************************************

Properties tablosunda

bir tablonun PROP_XXX değişkenlerini  başka bir tablo için oluşturmak için


INSERT INTO [dbo].[MS_PROPERTIES]
           ([TABLENAME]
           ,[FIELDNAME]
           ,[ROW_TYPE]
           ,[ROW_CATEGORY_NO]
           ,[ROW_LINE_NO]
           ,[ROW_CAPTION]
           ,[ROW_FIELDNAME]
           ,[ROW_FIELDTYPE]
           ,[ROW_COLUMN_TYPE]
           ,[LIST_TYPES_NAME])


SELECT 'MS_VARIABLES' --[TABLENAME]
      ,[FIELDNAME]
      ,[ROW_TYPE]
      ,[ROW_CATEGORY_NO]
      ,[ROW_LINE_NO]
      ,[ROW_CAPTION]
      ,[ROW_FIELDNAME]
      ,[ROW_FIELDTYPE]
      ,[ROW_COLUMN_TYPE]
      ,[LIST_TYPES_NAME]
  FROM [SystemMS].[dbo].[MS_PROPERTIES]
  where 0 = 0
  AND TABLENAME = 'MS_FIELDS_IP'
  and FIELDNAME = 'PROP_SEARCH'
  
       





**********************************************

SELECT DISTINCT GC_TURU FROM STK_GCB

UPDATE STK_GCB SET
GC_TURU = GC_TURU + 1000
WHERE GC_TURU > 0


UPDATE STK_GCS SET
GC_TURU = GC_TURU + 1000
WHERE GC_TURU > 0






**********************************************

DECLARE @sqlVariable nvarchar(15)
SET @sqlVariable = '1234567890'

SELECT
  @sqlVariable,
  REPLICATE('0', 15),
  '000000000000000' + @sqlVariable,
  RIGHT('000000000000000' + @sqlVariable, 13)

SELECT STUFF(@sqlVariable, 1, 0, REPLICATE('0', 15 - LEN(@sqlVariable)))
SELECT RIGHT(REPLICATE('0', 15) + LTRIM(RTRIM(@sqlVariable)), 15)



**********************************************




 .NET Framework ortamında dosya okuma ve yazma işlemleri için stream (akım veya diğer adı ile kanal)’den faydalanırız. Kanallar byte düzeyinde bir veri girdisi oluşturan veya çıktı bilgisi elde eden mantıksal birimlerdir.

Örneğin bir dosyadan byte düzeyinde bir veri almak için FileStram sınıfı kullanılır. StreamReader ve StreamWriter sınıfları ile de metin tabanlı bir dosyadan okuma ve yazma işlemleri yapabiliriz. Yine BinaryReader ve BinaryWriter sınıfları ile binary formatındaki dosyalar üzerinde işlemler yapabiliriz.

Kanal sınıflarının temelini System.IO.Streamsınıfı oluşturmaktadır, bu sınıfın bazı temel metotları şunlardır :

Close() : Kanalı kapatır, kaynakları serbest bırakır.
Read() : Üç adet parametresi vardır; Buffer, offset ve count. Byte sayısı kadar kanaldan okumayaparak buffera offset değerinden itibaren aktarılır.
ReadByte() : Kanaldan bir byte veri okur ve tam sayı geri döndürür, kanalın sonuna gelince -1 döndürür.
Seek() : İki parametresi vardır; offset ve origin. Kanal içindeki mevcut konumu offset değeri kadar öteler.
Flush() : Kanalın içeriğini bufferdan alıp fiziksel ortama (dosyaya vs) yazdırır.
Write() : Üç parametresi vardır; Buffer, offset ve count. Buffer offset değerinden başlanaraj count değeri kadar kanala gönderilir.
WriteByte() : Parametre olarak almış olduğu değeri kanala yazdırır.
Stream sınıfının bazı özelliklerine gelirsek :
CanWrite : Kanala yazma yapılabiliyorsa, true değerini alır.
CanRead : Kanaldan okuma yapılabiliyorsa, true değerini alır.
CanSeek : Kanal konumlandırmayı destekliyor ise true değerini alır.
Position : Kanalın o anki konumunu long tipinde verir.
Length : Kanalın uzunluğunu long tipinde verir.

Stream sınıfından türeyen üç tür Byte kanal vardır :

BufferedStream : Kanal üzerinde okuma – yazma işleminde buffer bellek alanını kullanarak işlem yapar. Mantığı şudur, disk üzerinden okuma veya yazma yapacağı anda bunu direkt olarak diske byte byte okuyup yazmaktansa buffer üzerinde ayrılan alana veriyi öncelikle yazar veya veriyi buraya çeker buffer alanı dolunca diske yazma işlemini gerçekleştirir ya da diskten tekrar okuma yapar. (işlem ne ise ona göre yani) Böylelikle performans yüksektir.

FileStream : Dosyalar üzerinde okuma-yazma işlemleri için kullanılır.

MemoryStream : Veri depolaması için direkt olarak belleği kullanmamıza yarar.

Bu byte kanal sınıfları System.IO.Stream sınıfından türemiştir dolayısı ile tüm özelliklerini taşımaktadırlar.

Karakter Kanalı için ise aşağıdaki sınıfları kullanabiliriz. Bu sınıflar TextWriter ve TextReader sınıflarından türemiştir.

StreamReader : Byte türündeki kanaldan karakteri okur.
StreamWriter : Byte türündeki kanala karakteri yazar.
StringReader : String türündeki bir nesneden karakterleri okur.
StringWriter : String türündeki bir nesneye karakteri yazar.


TextReader sınıfının metotları :

Close() : Kanalı oluşturan kaynağı kapatır.
Read() : Kanaldan bir karakterlik okuma yapar. Okuduğu karakterin tamsayı gösterimini geri döndürür. Okunacak karakter bittiğinde -1 döner.
ReadBlock() : Üç parametresi vardır; count, buffer ve index. Kanaldan count parametresi kadar karakteri okur, buffer parametresindeki diziye index parametresinden itibaren yazar.
ReadLine () : Kanaldaki bir sonraki metin satırını okur. Bunu string olarak geri döndürür. Veri bitince geriye null döner.
ReadToEnd() : Kanaldaki geri kalan tüm karakterleri okur, geriye string döner, veri bitince null döner.
Peek() : Giriş kanalından bir karakter okur, fakat karakteri giriş kanalından çıkarmaz. Eğer okunacak veri yoksa -1 döner.

TextWriter sınıfının metotları :

Close() : Kanalı kapatır.
Write() : Yeniden yüklenmiş olup aldığı parametre içeriğini çıkışa aynı satırda yazar. Sonuna satır sonlandırıcı karakterini eklemez.
WriteLine() : Yeniden yüklenmiş olup aldığı parametre içeriğini çıkışta yeni bir satır üzerinde yazar.
Flush() : Çıktıdaki verileri fiziksel ortama yazar.

Son olarak binary veriler içinde BinaryReader ve BinaryWriter sınıfları vardır.


varbinary ----------------------------------

DECLARE @BinaryVariable2 BINARY(20)

SET @BinaryVariable2 = 791
SET @BinaryVariable2 = @BinaryVariable2 + 1

SELECT CAST( @BinaryVariable2 AS varchar(max))
SELECT CAST( @BinaryVariable2 AS int )

select convert( VARBINARY(max), 'TestCase_TestCheck01_Mail_Validation')
select convert( varchar(max)  ,  convert(VARBINARY(max), 'TestCase_TestCheck01_Mail_Validation') )

---------------------------------------------------------------------------------------------------



declare @count int 
declare @say int = 1

SELECT @count = count(Id) FROM dbo.OnmBelgeMakbuz Where BelgeSeri = 'TbmGd'

WHILE ( @say <= @count ) 
BEGIN  
   print @say


/*
   UPDATE Production.Product  
      SET ListPrice = ListPrice * 2  
   SELECT MAX(ListPrice) FROM Production.Product  
   IF (SELECT MAX(ListPrice) FROM Production.Product) > $500  
      BREAK  
   ELSE  
      CONTINUE  
*/
  set @say = @say + 1
END 



/*

Full HD çözünürlük (1920x1080 piksel) ilk olarak 2006 yılında yaygınlaşmaya başladı.

Önemli Gelişmeler:
2005-2006: İlk ticari Full HD (1080p) televizyonlar piyasaya çıktı (örneğin, Sony, Sharp ve diğer markalar).

2006: Blu-ray Disc formatıyla birlikte 1080p içerik yaygınlaştı.

2007-2008: Full HD monitörler ve dizüstü bilgisayarlar daha erişilebilir hale geldi.

Özellikle 2007-2008 dönemi, Full HD'nin hem TV'lerde hem de bilgisayar monitörlerinde standart haline gelmeye başladığı yıllardı.

*/



-- Login oluştur
CREATE LOGIN [TABIM]
WITH PASSWORD = '312',
     CHECK_POLICY = OFF,   -- Basit şifre olduğu için policy kapatıldı
     CHECK_EXPIRATION = OFF;

-- İlgili veritabanında kullanıcı oluştur
USE [Surucu07];  -- Buraya kendi veritabanı adını yaz
CREATE USER [TABIM] FOR LOGIN [TABIM];

-- Yetki vermek istersen örnek:
ALTER ROLE db_datareader ADD MEMBER [TABIM];  -- Okuma yetkisi
ALTER ROLE db_datawriter ADD MEMBER [TABIM];  -- Yazma yetkisi

ALTER ROLE db_owner ADD MEMBER [TABIM];


